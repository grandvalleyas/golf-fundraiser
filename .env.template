# MongoDB Connection
# Replace with your MongoDB connection string (e.g., from MongoDB Atlas)
MONGODB_URI=
# Stripe Configuration
# Your Stripe secret key (from Stripe Dashboard -> Developers -> API Keys)
STRIPE_SECRET_KEY=
# Your Stripe webhook secret (from Stripe Dashboard -> Developers -> Webhooks)
STRIPE_WEBHOOK_SECRET=
# Clerk Authentication
# Public key from Clerk Dashboard -> API Keys -> Publishable Key
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
# Sign-in URL (can be left as-is or customized)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
# Sign-up URL (can be left as-is or customized)
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
# After sign-in redirect URL (where users go after signing in)
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/register
# After sign-up redirect URL (where users go after signing up)
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/register

# Cloudinary
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
NEXT_PUBLIC_CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=